extends ../layouts/master

block title
  title Swan Hellenic | FAQ

block basicSeo
  meta(content="This page FAQ" name="description")
  meta(content="Page, FAQ" name="keywords")

block manifestBrowserconfigFiles
  link(href="../manifest.json" rel="manifest" crossorigin)
  meta(content="../browserconfig.xml" name="msapplication-config")

block content
  main.section-not-green
    include ../mixins/breadcrumbs
    include ../mixins/heading
    section.gapper.section.section-white.section-has-sticky
      .wrapper
        +breadcrumbs([{type: 'span', title: 'FAQ'}])
        +heading('FAQ', '', 'faq', 'heading__icon-faq')
        include ../components/faq