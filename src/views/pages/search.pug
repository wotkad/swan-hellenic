extends ../layouts/master

block title
  title Swan Hellenic | Search

block basicSeo
  meta(content="This page Search" name="description")
  meta(content="Page, Search" name="keywords")

block manifestBrowserconfigFiles
  link(href="../manifest.json" rel="manifest" crossorigin)
  meta(content="../browserconfig.xml" name="msapplication-config")

block content
  main.section-not-green
    include ../mixins/breadcrumbs
    include ../mixins/heading
    include ../mixins/card
    section.gapper.section.section-white
      .wrapper
        +breadcrumbs([{type: 'span', title: 'Search'}])
        +heading('Antarctic','heading-sm heading-mb', '')
        .grid.grid__gap.grid__gap-big.grid-three
          +card('with status', 'with logos', '')
          +card('with status', 'with logos', 'sale')
          +card('with status', 'with logos', 'disabled')
          +card('with status', 'with logos', 'sale')
          +card('with status', 'with logos', '')
          +card('with status', 'with logos', 'sale')
          +card('with status', 'with logos', '')
          +card('with status', 'with logos', 'sale')
          +card('with status', 'with logos', '')
        +button('button-green-transparent cruises__button', 'button', 'Load more', '' ,'')