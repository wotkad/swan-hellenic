mixin button(className, buttonType, title)
  if buttonType
    button.button(type="" + buttonType class="" + className)
      .button__text= title
  else
    a.button(href="#" class="" + className)
      .button__text= title