mixin heading(title, className, type)
  .heading(class="" + className)
    h1.heading__title= title
    if type == "cruises"
      button.heading__icon(type="button" aria-label="filter")
        svg(width="32px" height="32px" viewBox="0 0 32 32")
          use(xlink:href="#svg-filter")