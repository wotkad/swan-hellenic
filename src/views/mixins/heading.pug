mixin heading(title, className, type)
  .heading(class=""+className)
    h1.heading__title= title
    if type == "cruises"
      button(aria-label="filter" type="button").heading__icon
        <svg width="32px" height="32px" viewBox="0 0 32 32"><use xlink:href="#svg-filter"></use></svg>